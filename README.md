# blo-png

Built on top of [blo](https://github.com/bpierre/blo), instead of generating a svg base64 uri, this lib generates a png base64 uri, so you can use it in react native or the other weird places that don't support svg.

## Installation

```bash
npm install blo-png
```
